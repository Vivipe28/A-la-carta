export class Idish {
    id!: number;
    title!: string;
    image!: string;

    constructor(id: number, title:string, image:string){
        this.id = id;
        this.image = title;
        this.image = image
    }
}